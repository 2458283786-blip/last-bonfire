# 最后的篝火

2D 横版建造经营 + 探索战斗游戏（开发中）。

游戏氛围与循环参考《王国两位君主》，玩家战斗参考《小骨英雄杀手》：玩家在城镇发展居民与建筑、在白天经营，夜晚抵御怪物；通过城镇中的地下通道进入探索关卡打怪，获得金币与怪物材料，不断重建与扩张。

## 环境要求

- Godot 4.7.1（本机为 Steam 版：`D:\steam\steamapps\common\Godot Engine`）

## 运行方式

- 用 Godot 打开本目录的 `project.godot`，按 F5 运行。
- 或命令行：`godot --path .`
- 当前入口场景：`scenes/main/boot.tscn`（按 Enter 进入城镇占位场景）

## 存档

- 自动存档：城镇内每天天亮（新的一天开始）自动保存。
- 手动存档/读档：Esc 暂停菜单 → 存档 / 读档。
- 启动界面：有存档时显示"继续游戏"。
- 存档位置：`user://save_game.json`（JSON 单文件，含版本号）。
- 读档：进入城镇时显示进度条，分帧恢复建筑/资源/居民，完成后村民自动恢复行动。

## 操作说明

| 按键 | 功能 |
| --- | --- |
| A / ← | 左移 |
| D / → | 右移 |
| 空格 / W / ↑ | 跳跃 |
| J / 鼠标左键 | 近战攻击 |
| K / 鼠标右键 | 弓箭射击 |
| E | 与附近可交互物互动（地下通道等） |
| B | 打开/关闭建造菜单 |
| Tab | 打开/关闭居民面板 |
| I | 打开/关闭背包（装备/物品） |
| Esc | 暂停/继续 |
| F9 | 调试：推进一天 |
| F10 | 调试：推进一个昼夜阶段 |

## 运行测试

```bash
godot --headless --path . res://tests/run_all.tscn
```

支持单跑某个测试（`--only=test_xxx`，xxx 为测试脚本名）：

```bash
godot --headless --path . res://tests/run_all.tscn -- --only=test_day_manager
```

## 目录结构

```
├── docs/           # 设计文档与开发日志
├── assets/         # 原始素材（美术/音频/字体）
├── scenes/         # Godot 场景（main/town/dungeon/player/villagers/enemies/buildings/ui）
├── scripts/        # GDScript（autoload 单例 + 各系统模块）
├── resources/      # 配置数据（.tres）与材质资源
└── shaders/        # 着色器源码
```

各目录的详细约定见对应文件夹内的 `README.md`。

## 开发规范

1. **数据驱动**：建筑、居民、怪物、武器等一律使用 `resources/data/` 下的配置，禁止写死在脚本里。
2. **场景与逻辑分离**：`.tscn` 放场景，`.gd` 放脚本（`scenes/` 与 `scripts/` 一一对应）。
3. **系统解耦**：跨系统通信走 `EventBus` 信号，不直接互相引用。
4. **玩法优先**：先跑通"探索 → 战斗 → 回城 → 建造 → 夜晚防御"核心循环，再扩展内容。
5. **预留接口**：所有系统保留等级/类型/属性/状态扩展位，避免后期重构。
6. **先设计后实现**：新功能先更新 `docs/design/` 设计文档，再动手写代码。

## 当前状态

- [x] 项目框架与目录结构
- [x] Godot 4.7 项目配置与全局单例（EventBus / GameManager / SaveManager / AudioManager）
- [x] 启动场景 → 城镇场景切换链路
- [x] 玩家移动、跳跃、近战攻击与弓箭射击
- [x] 玩家动画状态机与血条（受击无敌 / 死亡回篝火，篝火被毁回临时堆点）
- [x] 时间系统（DayManager：白天/黄昏/夜晚，F9/F10 调试推进）
- [x] 库存与经济（EconomyManager：开局库存、容量、出入库）
- [x] 资源双链：木头（伐木工/伐木屋/伐木场）+ 石头（矿工/矿工小屋/采石场）
- [x] 资源节点（树/石头：无碰撞、按天重生、数据驱动配置）
- [x] 居民自动采集（砍/挖 → 捡掉落物 → 运回仓库 → 入库）
- [x] 居民系统（自动/手动转职每日一次、受伤休养、无职业漫游）
- [x] 建筑（仓库/伐木屋/伐木场/矿工小屋/采石场 + 篝火核心；放置校验、详情、修复/重建/拆除）
- [x] 建筑损坏联动（被毁时容量失效 / 名额释放 / 场内资源清理，重建恢复）
- [x] 野外树随机限量生成（按天补足）
- [x] 敌人系统（夜狼：追击/攻击/死亡掉落，攻击优先级可配置）
- [x] 夜晚波次（强度随天数递增）
- [x] 城市内 UI：顶部状态栏（资源/天数/昼夜/血条/容量）
- [x] 建造菜单与放置（选建筑 → 校验 → 扣库存 → 生成）
- [x] 建筑详情面板（修复/重建/拆除）
- [x] 居民面板与每日一次职业调整
- [x] 交互提示 / 夜晚预警 / 通知队列
- [x] 暂停菜单
- [x] 存档系统（天亮自动存档 / 暂停菜单存读档 / 启动继续游戏，JSON 版本化）
- [x] 自动化测试（tests/，84 个测试脚本全绿，headless 可跑）
- [x] 建筑升级实际生效（容量/名额/资源数随等级提升；升级消耗含怪物材料）
- [x] 住宅系统（没房子不能工作；夜晚回家亮灯；被毁释放居民）
- [x] 居民威胁感知与撤退（怪物生成广播威胁，撤退回家 1.2 倍速；受伤 3 天）
- [x] 居民个体差异（移速 ±10%）与空闲小动作（生活感第一层）
- [x] DebugManager 统一调试开关（蓝图解锁/跳过造价等）
- [x] 防御建筑：民兵营/箭塔 + 士兵站桩 AI（近战/远程，受伤复用居民机制）
- [x] 背包最简版本（装备槽 + 物品列表，数据驱动）
- [x] 商店建筑（蓝图默认锁定，调试期可强制解锁）+ 装备数值（攻击/防御）与换装联动
- [x] 临时人口招募（金币消耗/冷却/人口上限，调试门控，预留正式解锁位）
- [x] 地下城时间限制（探索超时强制入夜，晚归波次加强；关卡内"距天黑"倒计时）
- [x] 夜袭强度动态公式（天数 + 居民/建筑/囤积加成 ± 随机浮动，系数可配待调参）
- [x] 夜晚波次从右侧屏幕外刷怪带生成并行军进入城镇（不会在城内凭空出现；城镇范围可配）
- [x] 地下城"两门二选一"结构（阶段制 + 左右两门选房间；阶段末强制 BOSS，通关回城）
- [x] 居民救援房（概率出现、每局上限；救回后城镇生成无职业居民）
- [x] 商人固定阶段解锁（第 2 阶段 BOSS 后正式解锁商店蓝图）
- [ ] 更多房间类型/事件房/深层装备解锁链
- [ ] 装备词条/稀有度与更多背包功能
- [ ] 更多怪物类型 / 建筑 / 资源与数值平衡

详细设计见 [docs/design/2026-08-10-master-plan.md](docs/design/2026-08-10-master-plan.md)（总设计方案，含全部子系统设计与数值总表）。
